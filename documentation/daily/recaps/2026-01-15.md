# Recap for 2026-01-15.

- Updated `/.devcontainer/scripts/validate-env.sh` to include the “extra” variables used by the devcontainer and helper scripts, and to validate more optional variables when set.
- Updated `/.env.example` so it matches the variables expected by the devcontainer setup (including `SSH_AUTH_SOCK` and the `ANSIBLE_*` path variables).
- Updated `/.devcontainer/scripts/README.md` to reflect the expanded validation coverage.

