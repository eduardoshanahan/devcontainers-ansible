# State for 2026-01-15.

- Current: `.env.example` now includes the extra variables used by devcontainer/run scripts (SSH agent socket, git sync knobs, Ansible path variables, and image retention days).
- Current: `.devcontainer/scripts/validate-env.sh` validates `ANSIBLE_CONFIG`, `ANSIBLE_INVENTORY`, `ANSIBLE_COLLECTIONS_PATH`, and `ANSIBLE_ROLES_PATH` as required variables (matching `.devcontainer/devcontainer.json` usage).
- Current: Optional env vars for git tooling and maintenance (`GIT_SSH_HOST`, `GIT_SYNC_REMOTES`, `GIT_SYNC_PUSH_REMOTES`, `DEVCONTAINER_IMAGE_RETENTION_DAYS`) are validated when present.
- Next: If you rely on a custom `.env`, compare it against `.env.example` after pulls so required values don’t drift.

