# Recap for 2026-01-09.

- Updated `/.devcontainer/devcontainer.json` to pass `ANSIBLE_CORE_VERSION`, `ANSIBLE_LINT_VERSION`, and `YAMLLINT_VERSION` as build args (matching `.env.example` and the validator).
- Updated `/.devcontainer/Dockerfile` to install the Ansible toolchain using those pinned build-arg versions.
- Improved `/.devcontainer/scripts/env-loader.sh` debug output to track newly added variable names (not changed values), with an opt-in flag to print values.
- Updated `/.devcontainer/config/starship.toml` to show `CONTAINER_HOSTNAME` (e.g. `project@hostname`) and moved the `->` separator to the main format.
- Updated `/Dockerfile` to require pinned Ansible toolchain versions (fail-fast instead of silently installing latest).
- Added ADRs to document the `lm/` daily journal, devcontainer `.env` configuration, SSH agent forwarding, and toolchain version pinning.
