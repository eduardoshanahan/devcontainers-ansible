# User configuration
HOST_USERNAME=vscode
HOST_UID=1000
HOST_GID=1000

# Git configuration
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your.email@example.com"

# Editor configuration
EDITOR_CHOICE=code  # Use 'code' for VS Code or 'cursor' for Cursor

# Docker configuration
DOCKER_IMAGE_NAME=ansible-dev
DOCKER_IMAGE_TAG=latest
DOCKER_CONTAINER_NAME=ansible-dev-env

# Project information - It is better to use the autocreated values from VS Code
PROJECT_NAME=ansible-devcontainer
PROJECT_VERSION=0.1.0

# Optional Docker settings
# DOCKER_REGISTRY=your-registry.com
# DOCKER_USERNAME=your-username
# DOCKER_BUILD_ARGS="--build-arg ARG1=value1 --build-arg ARG2=value2"

# Optional Ansible settings
# ANSIBLE_CONFIG=./ansible.cfg
# ANSIBLE_VAULT_PASSWORD_FILE=~/.ansible/vault_pass
